# Point these at wherever your services are actually running.
# Docker Compose defaults (see docker-compose.yaml in the backend project):
VITE_USERS_API=http://localhost:3001/api/users
VITE_LISTINGS_API=http://localhost:3002/api/listings
VITE_CHAT_API=http://localhost:3003/api/chat

# If you're running on Kubernetes instead (NodePort ports), use these instead:
# VITE_USERS_API=http://localhost:30001/api/users
# VITE_LISTINGS_API=http://localhost:30002/api/listings
# VITE_CHAT_API=http://localhost:30003/api/chat
